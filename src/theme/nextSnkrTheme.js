import { createTheme } from '@mui/material';

export const nextSnkrTheme = createTheme({
  palette: {
    primary: {
      main: '#262254'
    },
    secondary: {
      main: '#543884'
    },
    error: {
      main: '#ff1744'
    }
  }
});